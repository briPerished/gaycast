local render = {
    raycast = require("modules/render/raycast"), --gotta do path from perspective of main or else it screams
    textureManager = require("modules/render/textureManager")
}

return render